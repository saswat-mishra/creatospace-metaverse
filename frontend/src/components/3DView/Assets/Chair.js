/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useEffect, useRef } from "react";
import { useGLTF } from "@react-three/drei";
export default function ChairJsx({ pos, ...props }) {
  const group = useRef();
  const { nodes, materials, scene } = useGLTF("/chair-comp.glb");
  scene.traverse((node) => {
    if (node.isMesh) {
      node.castShadow = true;
    }
  });
  useEffect(() => {
    group.current.castShadow = true;
  }, []);
  return (
    <group ref={group} {...props} dispose={null} position={pos} castShadow>
      <group rotation={[-Math.PI / 2, 0, 0]} scale={0.97} castShadow>
        <group rotation={[Math.PI / 2, 0, 0]} castShadow>
          <group scale={0.01} castShadow>
            <mesh
              geometry={nodes.ChairSeat_LP4_lambert1_0.geometry}
              material={materials.lambert1}
              castShadow
            />
          </group>
        </group>
      </group>
    </group>
  );
}

useGLTF.preload("/chair-comp.glb");
